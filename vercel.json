{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/irpreneur2025(?:/(.*))?$", "dest": "/irpreneur2025/index.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
